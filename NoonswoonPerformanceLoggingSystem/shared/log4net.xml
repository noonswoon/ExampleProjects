<?xml version="1.0" encoding="utf-8" ?>
<log4net>
  <!--must be unique name-->
  <!--more http://logging.apache.org/log4net/release/sdk/log4net.Appender.ColoredConsoleAppender.html-->

  <appender name="QueueAppender" type="Noonswoon.Appender.MessageQueueAppender, Noonswoon.Appender">

    <bufferSize value="100" />

    <layout type="log4net.Layout.SimpleLayout"/>

    <filter type="log4net.Filter.LoggerMatchFilter">
      <!-- ...but not the rest of it -->
      <loggerToMatch value="Noonswoon.LoggingToDbWorkerRole" />
      <acceptOnMatch value="false" />
    </filter>

    <filter type="log4net.Filter.LoggerMatchFilter">
      <!-- ...but not the rest of it -->
      <loggerToMatch value="Noonswoon.Appender" />
      <acceptOnMatch value="false" />
    </filter>

  </appender>

  <!--for Yet Another Log4Net Viewer -->
  <appender name="FileAppenderXmlLayout" type="log4net.Appender.FileAppender">
    <!--not lock file-->
    <lockingModel type="log4net.Appender.FileAppender+MinimalLock" />
    <file type="log4net.Util.PatternString" value="C:\cloud.log.xml" />
    <appendToFile value="true" />

    <layout type="log4net.Layout.XmlLayoutSchemaLog4j">
      <locationInfo value="true" />
    </layout>
    <param name="Encoding" value="utf-8" />
  </appender>

  <appender name="FileAppender" type="log4net.Appender.FileAppender">
    <layout type="log4net.Layout.PatternLayout">
      <conversionPattern value="%date %thread [%-5level] %-10.30logger %message%newline%exception" />
    </layout>

    <file value="c:\cloud.log.txt"/>
    <!--current directory of process -->
    <appendToFile value="true"/>
    <lockingModel type="log4net.Appender.FileAppender+MinimalLock" />
  </appender>
  <!--default for all logger-->
  <root>
    <level value="DEBUG"/>
    <appender-ref ref="FileAppender"/>

    <appender-ref ref="FileAppenderXmlLayout"/>

    <appender-ref ref="QueueAppender"/>
  </root>
</log4net>